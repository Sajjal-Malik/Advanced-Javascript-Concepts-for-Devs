let servId = 12;

let url = `/service/${servId}`;

console.log(url);


let multiLineString = `
This sentence 
can be on multi 
    lines now
        possible with the
            help of template leterals using
            'back ticks'
`;

console.log(multiLineString);